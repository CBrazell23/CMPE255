import numpy as np

randomRows1 = np.random.choice(range(10), 3, replace=False)
randomRows2 = np.random.choice(range(10), 3, replace=False)
randomRows3 = np.random.choice(range(10), 3, replace=False)

print(randomRows1)
print(randomRows2)
print(randomRows3)
